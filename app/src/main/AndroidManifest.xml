<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hydramail">

    <application
        android:allowBackup="true"
        android:icon="@drawable/hydra_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ChooseAccount"></activity>

        <activity
            android:name=".ComposeMail"
        android:label="Compose Mail"
        android:launchMode="singleTask"
        android:parentActivityName=".ChooseAccount">

        <!-- The meta-data tag is required if you support API level 15 and lower -->
        <meta-data
            android:name="android.support.PARENT_ACTIVITY"
            android:value=".StartActivity" /></activity>



        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".StartActivity" />
        <activity android:name="Fragments.MainActivity" />
    </application>

</manifest>